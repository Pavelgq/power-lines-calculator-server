export class ActionControllers {

  async createNewAction(res, req) {

  }

  async getUserActions(res, req) {
    
  }

  async getAllActions(res, req) {
    
  }

  async authorizeAction(res, req) {
    
  }
}